# 贡献指南

欢迎提交可复现的问题和范围明确的改动。讨论缺陷时请提供系统、Go/Codex 版本、复现步骤、预期行为与实际结果；日志请使用脱敏片段。

## 本地开发

安装 `go.mod` 要求的 Go 版本、Make 和 Python 3，在仓库根目录运行：

```sh
make check
```

此命令执行格式检查、`go vet ./...`、`bash -n service.sh`、带竞态检测的单元测试与构建。测试自身和 Python 外层均设置 60 秒超时。只跑测试使用 `make test`，构建使用 `make build`。修改 Go 文件后对涉及文件执行 `gofmt -w`。

测试使用临时目录、模拟飞书 HTTP 服务与 shell 子进程，不调用真实模型，不读取生产配置，不需要飞书凭据或 Codex 登录。CI 在 macOS/Linux 运行同一套检查。真实飞书权限、客户端卡片展示和真实 Codex 版本兼容性需单独验收。

## 代码结构

| 位置 | 职责 |
|---|---|
| `main.go` | CLI 入口，报告错误并退出 |
| `internal/app` | 启动配置、飞书接入、消息校验与命令、任务生命周期、上下文、卡片、配置存储 |
| `internal/codex` | 执行请求与结果、CLI 事件解析、模型列表、进程组回收和环境变量隔离 |

依赖方向为 `main → app → codex`；Codex 包不依赖飞书 SDK。仅导出实际跨包使用的符号，不提供公共 Go SDK。

`bot.mu` 保护授权、存储、去重、会话和活动任务状态。接收消息时在锁内登记后台任务，执行前捕获配置与会话快照；网络与模型执行交给后台任务。停止接收后才等待后台任务结束，进程退出通过根上下文取消子进程。

配置存储由调用方持锁，使用临时文件、同步和原子替换；只有保存成功才更新内存。持久文件权限为 0600，用户配置目录为 0700；`.env` 的父目录权限由用户管理。

## 修改与测试原则

- 保持改动集中，不顺带添加未请求功能或通用抽象。
- 保留现有聊天命令、环境变量、配置路径、旧 JSON 兼容、会话隔离和权限边界。行为变更需明确说明触发条件及前后差异。
- 复杂缺陷先增加失败测试，再做最小修复；普通重构迁移原测试并补实际跨模块边界断言。
- 覆盖失败路径：保存失败不改变状态、撤销授权后不继续回复、进程超时能回收、上下文不能越界、卡片失败不重跑任务。
- 外部输入在边界验证；进程参数使用参数数组，提示词经 stdin 传递，凭据不写入源码。
- PR 说明具体问题、改后行为和已执行的验证；注明未完成的真实服务验收。

## 文档与运行文件

README 面向首次使用者，详细操作维护在 `docs/usage.md`。`.docs/plans/` 为历史实施记录，不作为当前行为规范。

不要提交 `.env`、`.run/`、`bin/` 或真实运维日志。程序不自动执行部署、发布或数据迁移；真实服务验收应使用明确授权的应用和环境。

代码采用 [MIT 许可证](LICENSE)。
